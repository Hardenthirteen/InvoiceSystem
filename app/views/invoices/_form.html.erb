<%= form_with(model: invoice) do |form| %>
  <% if invoice.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(invoice.errors.count, "error") %> prohibited this invoice from being saved:</h2>

      <ul>
        <% invoice.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <!-- 发票抬头 -->
  <div>
    <%= form.label :invoice_title, style: "display: block" %>
    <%= form.text_field :invoice_title %>
  </div>
  <!-- 发票税号 -->
  <div>
    <%= form.label :invoice_number, style: "display: block" %>
    <%= form.text_field :invoice_number %>
  </div>
  <!-- 开票人姓名 -->
  <div>
    <%= form.label :applicat_name, style: "display: block" %>
    <%= form.text_field :applicat_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>